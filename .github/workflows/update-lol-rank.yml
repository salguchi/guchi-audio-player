name: Update LoL current rank

on:
  schedule:
    - cron: '7,22,37,52 * * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/update-lol-rank.yml'
      - 'scripts/*.mjs'
      - 'lol-current-rank-client.js'

permissions:
  contents: write

concurrency:
  group: update-lol-rank
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Test current-rank extraction
        run: node --test scripts/*.test.mjs
      - name: Fetch OP.GG displayed Solo/Duo current rank
        run: node scripts/update-lol-rank.mjs
      - name: Save verified snapshot
        run: |
          git add -- lol-current-rank.json
          if git diff --cached --quiet -- lol-current-rank.json; then exit 0; fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Update OP.GG current rank snapshot'
          git pull --rebase origin main
          git push origin HEAD:main
